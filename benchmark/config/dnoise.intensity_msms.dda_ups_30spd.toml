# UPS intensity-threshold control, MS1+MS2 (both frame types collapsed to a pure
# per-point threshold). MS1 floor = min_window_intensity (T1), MS2 floor =
# msms_min_window_intensity (T2); both set per dataset by 36_ups_intensity_calib.py.
mz_half_width = 0
min_feature_length = 1
max_internal_gap = 0
min_window_intensity = 65
min_feature_intensity = 0
iterations = 1
halo = false
denoise_msms = true
msms_mz_half_width = 0
msms_min_feature_length = 1
msms_max_internal_gap = 0
msms_min_window_intensity = 59
msms_min_feature_intensity = 0
msms_iterations = 1
all_frames = false
