# UPS intensity-threshold control, MS1-only (collapses the vertical filter to a
# pure per-point threshold: keep iff intensity >= min_window_intensity). MS2
# untouched. T (min_window_intensity) is set per dataset by 36_ups_intensity_calib.py.
mz_half_width = 0
min_feature_length = 1
max_internal_gap = 0
min_window_intensity = 68
min_feature_intensity = 0
iterations = 1
halo = false
denoise_msms = false
all_frames = false
