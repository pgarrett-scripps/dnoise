[project]
name = "dnoise-benchmark"
version = "0.1.0"
description = "Benchmark dnoise's effect on DDA/DIA identifications and LFQ quant (PXD070049)"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
    "pyarrow>=24.0.0",
]
